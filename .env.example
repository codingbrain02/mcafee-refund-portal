VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-public-anon-key

# Store service role keys, database passwords, and banking credentials only in
# local environment variables, Vercel secrets, or a secrets manager.
SUPABASE_SERVICE_ROLE_KEY=server-only
SUPABASE_DB_PASSWORD=server-only
AUTHORIZED_BANK_API_BASE_URL=https://bank-api.example.com
AUTHORIZED_BANK_API_CLIENT_ID=server-only
AUTHORIZED_BANK_API_CLIENT_SECRET=server-only
